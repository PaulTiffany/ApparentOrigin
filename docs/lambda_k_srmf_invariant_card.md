# lambda_K SRMF Invariant Card

Status: Episode 4 theory-craft artifact.

Phase: near-cousin / theory discipline, not observed-number derivation, not
instantiation.

Purpose:

This document turns the `lambda_K` lane into an SRMF invariant card before any
dataset-facing prediction is allowed. It imports the invariant-map discipline
from `C:\src\principia\Invariants\SRMF_INVARIANT_MAP.md` into AOC.

The point is not to leave zero-order science behind. The point is to use
zero-order discipline to enter a second-order domain without cheating.

Local meaning of zero-order:

```text
Can the model reproduce, preserve, or respect standard observations?
```

Second-order AOC target:

```text
Can the model predict boundary behavior, pipeline-dependent floors,
or control-resistant residue that standard analysis does not naturally
organize?
```

The rule:

```text
Second-order claims are admissible only when zero-order controls are preserved.
```

## 1. Why an Invariant Card Is Needed

The current `lambda_K` work has one useful theory-side object:

```tex
\Lambda_K^{adm}(\chi,K_P)=h_K(\chi)/K_P.
```

From `docs/lambda_k_observable_feasibility_first_pass.md`, this is an
observable-feasibility scale, not an observed amplitude.

That type distinction prevents one failure mode. It does not prevent all of
them. AOC can still oopsie into:

1. black-box judgment,
2. hidden channel-response fitting,
3. cross-probe portability,
4. pretending a feasibility kernel is an observed value,
5. confusing a better story with a falsifiable prediction.

The SRMF invariant card is the guardrail against those failures.

## 2. SRMF Placement

Current location in the SRMF cycle:

```text
TTIE -> TTCS
```

Reason:

1. TTIE has integrated fragments into a typed theory surface:
   Kerr near-cousin, apparatus-bound `K_P`, Sprint E prism grammar,
   observable-versus-observed guardrail.
2. The next move is TTCS: sample or propose coherent candidate maps from that
   surface.
3. We are not yet at TTDC. There is no final test, release, or commitment until
   a candidate map has a diagnostic and falsification invariant.

Forbidden shortcut:

```text
TTIE surface -> TTDC verdict
```

Allowed next move:

```text
TTIE surface -> TTCS candidate map -> TTPR refinement -> TTDC test/refusal
```

## 3. Six Invariant Families for lambda_K

### State Invariant

Question:

```text
What typed object exists at this stage?
```

Current typed object:

```text
Kerr near-cousin feasibility kernel + apparatus-bound access budget
```

Formal objects:

```tex
h_K(\chi)=\frac{1-\sqrt{1-\chi^2}}{1+\sqrt{1-\chi^2}},
\qquad
\Lambda_K^{adm}(\chi,K_P)=h_K(\chi)/K_P.
```

Type:

```text
observable-feasibility structure
```

Not type:

```text
observed cosmological amplitude
```

Failure if absent:

The theory has no domain and collapses into analogy.

### Budget Invariant

Question:

```text
What bounded-observer constraint applies?
```

Budget:

```text
apparatus-bound K_P plus atlas-coherence controls
```

`K_P` is not an aesthetic scale. It is the finite reconstruction budget of a
specified observer/pipeline.

Minimum admissible statement:

```text
This map is valid only inside the feasibility band where the pipeline remains
measurable and atlas-coherent.
```

Failure if absent:

The operator overclaims unbounded access and treats inaccessible structure as
if it had been observed.

### Coherence Invariant

Question:

```text
What must be preserved across the operation?
```

Preserved distinctions:

1. feasible versus observable versus observed,
2. Kerr near-cousin versus AOC territory,
3. zero-order preservation versus second-order signal,
4. prism-specific map versus universal deformation,
5. apparatus-bound `K_P` versus fundamental `K_*`.

Failure if absent:

The output is no longer the same symbolic problem. A Kerr feasibility kernel
quietly becomes a Planck/Pantheon/DESI result.

### Diagnostic Invariant

Question:

```text
What scalar or report is computable without a judge?
```

Minimum judge-free diagnostic surface:

1. `h_K(chi)`,
2. `g_K(chi)=1-h_K(chi)`,
3. `K_P` from the chosen apparatus-bound pipeline,
4. `Lambda_K^adm(chi,K_P)`,
5. selected Sprint E prism,
6. predeclared channel-response coefficient or response family,
7. pass/fail outcome under the selected control.

No LLM judge, no aesthetic confidence, and no post-hoc "looks aligned" verdict
may substitute for these diagnostics.

Failure if absent:

The claim becomes rhetorical.

### Transition Invariant

Question:

```text
What obstruction forces the next operator?
```

Current obstruction:

```text
The feasibility kernel is not yet an observable-channel prediction.
```

Therefore:

```text
TTIE has saturated. TTCS is forced.
```

The next operator must propose a constrained candidate map:

```text
source feasible kernel -> selected prism -> channel response -> predicted
survival/collapse/recomposition
```

Failure if absent:

The cycle becomes optional workflow. The theory can remain indefinitely
interesting without becoming vulnerable.

### Falsification Invariant

Question:

```text
What observation would kill this operator claim?
```

For the next candidate map, falsification must be specified before observing
the target coordinate.

Valid falsifiers include:

1. predicted prism survival collapses under the predeclared control,
2. predicted prism collapse survives robustly,
3. predicted recomposition appears only after changing the prism definition,
4. channel response requires fitting to the target dataset,
5. zero-order controls are degraded.

Failure if absent:

The result cannot be tested and becomes a protected interpretation.

## 4. Oopsie Map: Where AOC Can Smuggle Black-Box Judgment

| Risk | How it enters | Invariant that blocks it |
| --- | --- | --- |
| black-box judgment | "the pattern looks like AOC" | diagnostic invariant |
| observed/observable type mismatch | feasibility kernel treated as measured value | state invariant |
| cross-probe portability | Pantheon+ amplitude exported to BAO/CMB | coherence invariant |
| unbounded observer leak | inaccessible pre-boundary structure narrated as seen | budget invariant |
| story-first confirmation | Kerr chosen because it sounds right | falsification invariant |
| endless method refinement | controls tighten forever without prediction | transition invariant |
| hidden channel fitting | `C_P` inferred after seeing target data | diagnostic + falsification invariants |
| zero-order violation | standard observations degraded by second-order ambition | budget + coherence invariants |

This table is not a scolding layer. It is where the theory becomes useful:
AOC's own primitive predicts these are exactly the places bounded observers
will confuse feasibility, observability, and observation.

## 5. Candidate TTCS Maps

These are candidate maps to sample next. None is committed here.

### Candidate A: Planck Operator-Prism Survival Map

Source:

```text
h_K(chi)/K_P
```

Prism:

```text
operator prism
```

Candidate prediction form:

```text
Kerr axial feasibility predicts survival of an axial residue coordinate across
operator decomposition, but not necessarily a specific Galactic direction.
```

Main risk:

Too vague unless the coordinate is specified before evaluation.

### Candidate B: Mask-Recomposition Map

Source:

```text
g_K(chi)/K_P
```

Prism:

```text
mask / sky-partition prism
```

Candidate prediction form:

```text
Finite-budget access to a two-horizon near-cousin predicts recomposition at
mask-family feasibility boundaries rather than smooth drift.
```

Main risk:

Could be retrofitted to any cliff unless the mask family and cliff statistic are
frozen first.

### Candidate C: Null BAO Map

Source:

```text
lambda_K admissible only for reconstruction operators, not standard-ruler mean
distances
```

Prism:

```text
observable-map / cross-probe prism
```

Candidate prediction form:

```text
BAO mean-vector deformation should collapse while operator-residue or
covariance/atlas signatures may remain admissible.
```

Main risk:

Can sound like explaining away DESI unless stated before the next BAO contact.

## 6. Best Next Contract

Recommended next theory contract:

```text
Planck operator-prism candidate map
```

Reason:

1. It respects the current strongest empirical machinery.
2. It does not reuse Pantheon+ fitted amplitude.
3. It can be phrased as survival/collapse/recomposition rather than direct
   cosmological amplitude.
4. It keeps `lambda_K` in observable-feasibility type until a channel response
   is specified.

Contract skeleton:

```text
Given h_K(chi)/K_P as a feasibility kernel, choose one Planck operator-residue
coordinate and predeclare whether Kerr-side axiality predicts survival,
collapse, or recomposition under the operator prism. State the null that would
falsify the map before reading the target value.
```

## 7. Relation to Zero-Order Discipline

AOC enters the second-order domain by respecting zero-order rules:

1. preserve standard observables unless a map says otherwise,
2. define diagnostics before interpretation,
3. use judge-free reports where possible,
4. state falsifiers before evaluation,
5. do not treat feasibility as observation,
6. do not export amplitudes across probes without a map.

This is how AOC uses the power of zero-order science to enter a domain that
zero-order science alone does not organize: boundary behavior of bounded
observers.

## Allowed Claims

1. The `lambda_K` lane now has an SRMF invariant card.
2. The current theory object is an observable-feasibility kernel, not an
   observed value.
3. The next valid move is a TTCS candidate map for one selected prism.
4. Black-box judgment and false portability are now named as invariant failures.

## Forbidden Claims

1. The invariant card derives `lambda_K`.
2. The invariant card confirms AOC.
3. AOC may bypass zero-order controls because it studies second-order boundary
   behavior.
4. A channel response may be chosen after seeing the target data.
5. Aesthetic or narrative fit may substitute for judge-free diagnostics.

