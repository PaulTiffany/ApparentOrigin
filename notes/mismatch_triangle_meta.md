# Meta-Evaluation: The Mismatch Triangle

## Summary
We attempted to model the **Mismatch Triangle** (SN-BAO-CMB) using three
different scaling laws for the Information-Geometric Capacity $\Omega_O$:
1. **First-Order Shannon Density ($n \cdot a^2$):** Overestimated SN-CMB tension by 10x.
2. **Linear Adaptedness (Omega):** Underestimated SN-CMB tension by 30x.
3. **Parameter-Level Precision ($1/\sigma^2$):** Overestimated SN-CMB tension by 10x.

## The Scientific Conflict
The "Ballpark" result (62% accuracy) from the earlier aggregate exponent $p \approx 2.0$ was the most successful, but it was still phenomenological. The subsequent attempts to "ground" the capacity $\Omega_O$ in either raw mode counts or marginal parameter precision have failed to replicate that accuracy.

### Why? (Independent Scientific Thought)
$H_0$ is not a fundamental parameter of the interface; it is a **projection**. 
- **Planck** doesn't measure $H_0$; it measures $\theta_*$ (angular scale) and projects it to $H_0$ via $\Lambda$CDM.
- **SH0ES** doesn't measure $H_0$; it measures $M_B$ (absolute magnitude) and projects it.

The "Residue" is not just a ratio of capacities; it is the **curvature mismatch** between the projection maps. 

## Candidate Insight: The Saturation Boundary
The **Spectral Plateau** we measured at $z \approx 0.5$ is the most robust data point we have. 
- The SN pipeline "stalls" its information gain at $z=0.5$.
- The BAO pipeline "starts" its high-precision regime at $z=0.5$.
- The **Atlas Fracture** occurs exactly where these two observers meet.

## Strategy Shift: Atlas Stitching, not Scaling
Instead of assuming a universal scaling law ($p$), we should look at the
**Local-to-Global Transition Map**.
- The tension isn't a "residue of altitude"; it is the **Residue of Stitching**. 
- We need to calculate the **Transition Residue** required to glue a $z \le 0.5$ atlas to a $z > 0.5$ atlas given their measured spectral volumes.

**Verdict:** The "Mismatch Triangle" was a useful falsification exercise. It
shows that simple capacity ratios are insufficient in the current formulation.
The stronger next path is the **Transition Map** at the candidate saturation
boundary.
