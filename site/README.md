# Static Site

Open `site/index.html` in a browser.

The site is intentionally static. It uses the existing generated images as
visual anchors and links back to the canonical proof, guardrail, simulation, and
media documents.

Design rule:

> The site is a research home base, not a claim of empirical victory.

